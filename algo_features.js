/*************************************************************
 * YOPO AI PRO — algo_features.js (server helper)
 * 역할:
 * - algo_core.js 보조 피처 모듈 (현재 버전: 최소 구성)
 * 주의:
 * - 브라우저(UI) 코드/자동 기능 로직은 절대 포함하지 않는다.
 *************************************************************/
"use strict";

// (현재 algo_core.js는 이 모듈을 직접 require 하지 않습니다.)
// 향후 확장 시, 순수 함수만 추가하세요.

module.exports = {};
